
  # File creation request

  This is a code bundle for File creation request. The original project is available at https://www.figma.com/design/ngyiq4ml3Iqa4WF3GJgEsq/File-creation-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  